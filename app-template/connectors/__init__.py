"""Third-party issue-tracker connectors - see base.py for the interface
every connector implements, so they're interchangeable from the issues
system's point of view. Jira (jira.py) is the only one built so far."""
